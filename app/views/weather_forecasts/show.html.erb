<%= turbo_stream_from(@location) if !@location.forecast %>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">
    Weather <%= '(cached)' if @location.forecast&.cached %>
  </h1>

  <div class="mx-0">
    <%= render @location %>
  </div>
</div>
